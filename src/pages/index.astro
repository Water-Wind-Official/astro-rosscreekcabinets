---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main id="home">
			<section class="hero-strip" aria-hidden="true">
				<img src="https://i.postimg.cc/CLLdWd5W/Gemini-Generated-Image-s6035cs6035cs603.png" alt="" loading="lazy" decoding="async" />
			</section>
			<section class="hero hero--banner section-flair section-flair--warm section-band section-band--amber">
				<div class="hero__content">
					<p class="eyebrow">Waycross, GA • Family owned</p>
					<h1>Cabinets built for the way you live.</h1>
					<p class="lede">
						Ross Creek Cabinets designs and installs kitchens, vanities, built-ins, and storage for South Georgia
						homes.
					</p>
					<p class="lede alt">
						Owner Derek Roberson measures, builds, and installs so every cabinet fits, functions, and looks right on
						day one.
					</p>
					<div class="hero__actions">
						<a class="btn ghost" href="#contact">Contact Us</a>
						<a class="btn ghost" href="#contact">Email</a>
					</div>
					<ul class="hero__highlights">
						<li>Made-to-measure layouts</li>
						<li>Built and installed locally by Derek</li>
						<li>Durable woods, finishes, and hardware</li>
					</ul>
				</div>
				<div class="hero__card">
					<h3>Meet your cabinetmaker</h3>
					<p>
						“Every project is personal. I measure, build, and install so you get cabinetry that fits right
						and looks right.”
					</p>
					<div class="hero__contact">
						<p>Derek Roberson</p>
						<p>Ross Creek Cabinets LLC</p>
						<p>Waycross, Georgia</p>
					</div>
				</div>
			</section>

			<section id="services" class="section-band section-band--linen">
				<div class="section-header">
					<p class="eyebrow">What we build</p>
					<h2>Cabinetry tailored to your rooms and routines.</h2>
					<p>
						From first sketch to final install, we design around your dimensions, finishes, and storage needs.
						We can coordinate with your contractor or manage the full cabinetry scope.
					</p>
				</div>
				<div class="card-grid">
					<div class="card">
						<h3>Kitchens</h3>
						<p>Full custom kitchens, islands, pantries, and trim that anchor your home.</p>
					</div>
					<div class="card">
						<h3>Bathrooms &amp; Laundry</h3>
						<p>Vanities, linen towers, and laundry storage built to handle daily use.</p>
					</div>
					<div class="card">
						<h3>Built-ins &amp; Living Spaces</h3>
						<p>Fireplace surrounds, media walls, mudrooms, and window seats that look original.</p>
					</div>
					<div class="card">
						<h3>Closets &amp; Storage</h3>
						<p>Closet systems, drop zones, and organization that keep everything in its place.</p>
					</div>
					<div class="card">
						<h3>Commercial Spaces</h3>
						<p>Reception desks, break rooms, and storage that work for busy offices.</p>
					</div>
					<div class="card">
						<h3>Finishes &amp; Hardware</h3>
						<p>Paint-grade or stained hardwoods, soft-close hardware, pull-outs, and organizers.</p>
					</div>
				</div>
			</section>

			<section id="process" class="section-band section-band--charcoal">
				<div class="section-header">
					<p class="eyebrow">How it works</p>
					<h2>A clear path from first call to final install.</h2>
				</div>
				<div class="steps">
					<div class="step">
						<div class="step__number">1</div>
						<div>
							<h3>Measure &amp; consult</h3>
							<p>We meet on-site in Waycross, GA, confirm dimensions, talk finishes, and align on budget.</p>
						</div>
					</div>
					<div class="step">
						<div class="step__number">2</div>
						<div>
							<h3>Design &amp; approval</h3>
							<p>Layouts and door styles are finalized; you review details before we start building.</p>
						</div>
					</div>
					<div class="step">
						<div class="step__number">3</div>
						<div>
							<h3>Build</h3>
							<p>We source materials, craft in the shop, pre-fit, and finish for a clean install.</p>
						</div>
					</div>
					<div class="step">
						<div class="step__number">4</div>
						<div>
							<h3>Install &amp; walkthrough</h3>
							<p>Cabinets are installed, aligned, and tuned. We walk through together and handle any touch-ups.</p>
						</div>
					</div>
				</div>
			</section>

			<section id="portfolio" class="showcase section-band section-band--slate">
				<div class="section-header">
					<p class="eyebrow">Recent work</p>
					<h2>Cabinetry sized for the room, styled for daily living.</h2>
					<p>
						A few in-progress shots from the shop and job sites. Each piece is measured, built, and finished by
						Derek so the fit and finish stay consistent from drawings to install.
					</p>
				</div>
				<div class="showcase-grid">
					<figure class="showcase-card showcase-card--wide">
						<img src="https://i.postimg.cc/59LjDS64/IMG-20251209-141756.jpg" alt="Light, warm kitchen cabinetry with a centered island" loading="lazy" />
						<figcaption>Kitchen run with a painted island, wide drawers, and balanced uppers.</figcaption>
					</figure>
					<figure class="showcase-card showcase-card--vertical">
						<img src="https://i.postimg.cc/QMXh2TYC/IMG-20251209-141801.jpg" alt="Tall built-in cabinetry with open shelves" loading="lazy" />
						<figcaption>Built-in shelving and base storage sized to the wall for a clean fit.</figcaption>
					</figure>
					<figure class="showcase-card showcase-card--vertical">
						<img src="https://i.postimg.cc/YjKwbGm0/IMG-20251209-141751.jpg" alt="Detailed cabinet doors staged for finishing" loading="lazy" />
						<figcaption>Door sets prefinished and staged before hardware and installation.</figcaption>
					</figure>
				</div>
			</section>

			<section id="about" class="panel section-flair section-flair--warm section-band section-band--butter">
				<div class="section-header">
					<p class="eyebrow">Who we are</p>
					<h2>Owner-led craftsmanship in Waycross, Georgia.</h2>
					<p>
						Ross Creek Cabinets LLC is owned and operated by Derek Roberson. Every project is measured,
						built, and installed with care so your cabinetry fits the room, functions for daily life, and
						looks good for years.
					</p>
				</div>
				<div class="two-col">
					<div class="card">
						<h3>Local and accountable</h3>
						<p>Located at 2116 Carswell Ave, Waycross, GA. We serve homeowners, remodelers, and builders across South Georgia.</p>
					</div>
					<div class="card">
						<h3>Communication first</h3>
						<p>One point of contact from estimate to install. You always know what’s next and when to expect it.</p>
					</div>
				</div>
			</section>

			<section id="contact" class="section-flair section-flair--quiet section-band section-band--creek">
				<div class="cta">
					<div>
						<p class="eyebrow">Ready to start?</p>
						<h2>Tell us about your project.</h2>
						<p>Call or email to schedule a site visit in Waycross or the surrounding South Georgia area.</p>
					</div>
					<div class="cta__actions">
						<a class="btn primary" href="#contact">912-337-1152</a>
						<a class="btn ghost" href="mailto:rosscreekcabinets@gmail.com">rosscreekcabinets@gmail.com</a>
						<p class="cta__address">2116 Carswell Ave • Waycross, GA</p>
						<div class="copy-row">
							<button class="copy-btn" type="button" data-copy="+19123371152" aria-label="Copy phone number">Copy phone</button>
							<button class="copy-btn" type="button" data-copy="rosscreekcabinets@gmail.com" aria-label="Copy email address">Copy email</button>
						</div>
					</div>
				</div>
			</section>
		</main>
		<Footer />
	</body>
</html>
<style>
	.hero {
		display: grid;
		grid-template-columns: 2fr 1fr;
		gap: 2em;
		align-items: center;
	}
	:root {
		--hero-lift: -120px;
	}
	.hero-strip {
		position: relative;
		margin: 0 calc(50% - 50vw);
		overflow: hidden;
		border-radius: 18px;
		box-shadow: 0 18px 34px rgba(var(--black), 0.18);
	}
	.hero-strip img {
		width: 100vw;
		max-width: 100%;
		height: 680px;
		display: block;
		object-fit: cover;
		object-position: center;
		filter: saturate(1.2) contrast(1.15) brightness(0.9);
		transform: scale(1.05);
		transition: transform 1.2s ease;
	}
	.hero-strip:hover img {
		transform: scale(1.08);
	}
	.hero-strip::after {
		content: "";
		position: absolute;
		inset: 0;
		background:
			linear-gradient(180deg, rgba(0, 0, 0, 0.2), rgba(0, 0, 0, 0.8)),
			radial-gradient(circle at 20% 20%, rgba(212, 175, 55, 0.2), rgba(212, 175, 55, 0) 60%),
			radial-gradient(circle at 70% 40%, rgba(255, 255, 255, 0.1), rgba(255, 255, 255, 0) 55%);
		pointer-events: none;
	}
	main > section:first-of-type {
		margin-top: 0;
	}
	.hero--banner {
		position: relative;
		transform: translateY(var(--hero-lift));
		margin-top: 0;
		margin-bottom: 0;
		padding: 3.5em 3em;
		overflow: hidden;
		border-radius: 0;
		box-shadow: 0 32px 80px rgba(0, 0, 0, 0.4), 0 0 0 1px rgba(212, 175, 55, 0.2);
		background:
			linear-gradient(135deg, rgba(0, 0, 0, 0.95), rgba(0, 0, 0, 0.98)),
			radial-gradient(circle at 20% 10%, rgba(212, 175, 55, 0.15), rgba(212, 175, 55, 0) 60%);
		border: 2px solid rgba(212, 175, 55, 0.3);
		z-index: 2;
	}
	.hero__content {
		background: linear-gradient(135deg, rgba(0, 0, 0, 0.9), rgba(20, 20, 20, 0.95));
		padding: 2.5em;
		border-radius: 0;
		border: 1px solid rgba(212, 175, 55, 0.2);
		box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3), inset 0 1px 0 rgba(212, 175, 55, 0.1);
		position: relative;
	}
	.hero__content::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 3px;
		background: linear-gradient(90deg, #D4AF37, #E6C357, #D4AF37);
	}
	.hero__content h1 {
		margin-bottom: 0.5em;
		max-width: 18ch;
		line-height: 1.1;
		letter-spacing: -0.03em;
		font-size: clamp(2.8rem, 5vw, 4.2rem);
		font-weight: 900;
		color: #FFFFFF;
		text-transform: uppercase;
		position: relative;
	}
	.hero__content h1::after {
		content: '';
		position: absolute;
		bottom: -8px;
		left: 0;
		width: 80px;
		height: 3px;
		background: #D4AF37;
	}
	.hero__content .lede {
		max-width: 45ch;
		margin-bottom: 0.8em;
		font-size: 1.15em;
		font-weight: 600;
		color: rgba(255, 255, 255, 0.95);
		line-height: 1.4;
	}
	.hero__content .lede.alt {
		color: rgba(212, 175, 55, 0.9);
		margin-top: -0.1em;
		font-weight: 500;
	}
	.hero__actions {
		display: flex;
		flex-wrap: wrap;
		gap: 1em;
		margin: 1.5em 0;
	}
	.hero__actions .btn {
		min-width: 200px;
		padding: 1em 1.5em;
		font-weight: 800;
		font-size: 0.95em;
		text-transform: uppercase;
		letter-spacing: 0.08em;
		border-radius: 0;
		transition: all 0.3s ease;
	}
	.hero__actions .btn.ghost {
		background: transparent;
		border: 2px solid rgba(212, 175, 55, 0.6);
		color: #D4AF37;
		font-size: 0.85em;
		min-width: 160px;
		padding: 0.8em 1.2em;
	}
	.hero__actions .btn.ghost:hover {
		background: rgba(212, 175, 55, 0.1);
		border-color: #D4AF37;
		color: #FFFFFF;
	}
	.hero__highlights {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
		gap: 0.8em;
		list-style: none;
		padding: 0;
		margin: 1.5em 0 0 0;
	}
	.hero__highlights li {
		background: linear-gradient(135deg, rgba(212, 175, 55, 0.1), rgba(212, 175, 55, 0.05));
		padding: 1em 1.2em;
		border-radius: 0;
		border: 1px solid rgba(212, 175, 55, 0.3);
		font-weight: 700;
		color: rgba(255, 255, 255, 0.95);
		text-transform: uppercase;
		letter-spacing: 0.05em;
		font-size: 0.9em;
		transition: all 0.3s ease;
	}
	.hero__highlights li:hover {
		background: linear-gradient(135deg, rgba(212, 175, 55, 0.2), rgba(212, 175, 55, 0.1));
		border-color: #D4AF37;
		transform: translateY(-2px);
	}
	.hero__card {
		background: linear-gradient(135deg, rgba(0, 0, 0, 0.95), rgba(20, 20, 20, 0.98));
		padding: 2em;
		border-radius: 0;
		box-shadow: 0 16px 32px rgba(0, 0, 0, 0.4), inset 0 1px 0 rgba(212, 175, 55, 0.1);
		border: 1px solid rgba(212, 175, 55, 0.3);
		position: relative;
	}
	.hero__card::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 2px;
		background: linear-gradient(90deg, #D4AF37, #E6C357, #D4AF37);
	}
	.hero__card h3 {
		margin-bottom: 0.6em;
		color: #D4AF37;
		font-weight: 800;
		text-transform: uppercase;
		letter-spacing: 0.05em;
		font-size: 1.1em;
	}
	.hero__card p {
		color: rgba(255, 255, 255, 0.9);
		font-weight: 500;
		line-height: 1.6;
	}
	.hero__contact {
		margin-top: 1.5em;
		padding-top: 1.5em;
		border-top: 1px solid rgba(212, 175, 55, 0.3);
		color: rgba(212, 175, 55, 0.8);
	}
	.hero__contact p {
		margin: 0.3em 0;
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: 0.05em;
		font-size: 0.85em;
	}
	.eyebrow {
		text-transform: uppercase;
		letter-spacing: 0.1em;
		font-weight: 900;
		font-size: 0.8em;
		color: #D4AF37;
		margin: 0 0 0.4em 0;
		position: relative;
		display: inline-block;
	}
	.eyebrow::after {
		content: '';
		position: absolute;
		bottom: -4px;
		left: 0;
		width: 40px;
		height: 2px;
		background: #D4AF37;
	}
	.section-header {
		text-align: left;
		max-width: 820px;
		margin-bottom: 3em;
		position: relative;
	}
	.section-header h2 {
		font-size: clamp(2.2rem, 3.5vw, 3rem);
		letter-spacing: -0.02em;
		font-weight: 900;
		color: #000000;
		line-height: 1.1;
		margin-bottom: 0.5em;
		text-transform: uppercase;
	}
	.section-header p {
		font-size: 1.1em;
		line-height: 1.6;
		color: rgba(0, 0, 0, 0.8);
		font-weight: 500;
	}
	.section-band--linen .section-header h2,
	.section-band--slate .section-header h2,
	.section-band--butter .section-header h2 {
		color: #FFFFFF;
	}
	.section-band--linen .section-header p,
	.section-band--slate .section-header p,
	.section-band--butter .section-header p {
		color: #D4AF37;
	}
	.section-band {
		position: relative;
	}
	.section-band::before {
		content: "";
		position: absolute;
		inset: -10% -9999px;
		background: var(--section-band-color, #f9f3e7);
		z-index: 0;
	}
	.section-band > * {
		position: relative;
		z-index: 2;
	}
	.section-band--amber {
		--section-band-color: linear-gradient(130deg, rgba(212, 175, 55, 0.15), rgba(255, 255, 255, 0.98));
	}
	.section-band--linen {
		--section-band-color: linear-gradient(180deg, #1a1a1a, #2d2d2d);
	}
	.section-band--creek {
		--section-band-color: linear-gradient(150deg, rgba(212, 175, 55, 0.08), rgba(255, 255, 255, 0.98));
	}
	.section-band--slate {
		--section-band-color: linear-gradient(180deg, #1a1a1a, #2d2d2d);
	}
	.section-band--butter {
		--section-band-color: linear-gradient(140deg, rgba(26, 26, 26, 0.85), rgba(45, 45, 45, 0.9));
	}
	.section-band--charcoal {
		--section-band-color: linear-gradient(180deg, #1a1a1a, #2d2d2d);
	}
	.section-band--charcoal .section-header h2 {
		color: #FFFFFF;
	}
	.section-band--charcoal .section-header p {
		color: #D4AF37;
	}
	.section-band--shell {
		--section-band-color: linear-gradient(135deg, rgba(255, 255, 255, 0.98), rgba(212, 175, 55, 0.08));
	}
	.card-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
		gap: 1.6em 1.8em;
		align-items: stretch;
	}
	.card {
		display: flex;
		flex-direction: column;
		padding: 2.2em 1.8em;
		opacity: 0;
		transform: translateY(18px);
		animation: riseFade 0.75s ease forwards;
		border-top: 4px solid #D4AF37;
		transition: all 0.3s ease;
		background: linear-gradient(135deg, rgba(255, 255, 255, 0.98), rgba(250, 250, 250, 0.95));
		border-radius: 0;
		box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1), 0 0 0 1px rgba(212, 175, 55, 0.1);
		position: relative;
		overflow: hidden;
	}
	.section-band--linen .card,
	.section-band--slate .card,
	.section-band--butter .card {
		background: linear-gradient(135deg, rgba(26, 26, 26, 0.95), rgba(45, 45, 45, 0.98));
		border: 1px solid rgba(212, 175, 55, 0.3);
		box-shadow: 0 8px 24px rgba(0, 0, 0, 0.4), 0 0 0 1px rgba(212, 175, 55, 0.2);
	}
	.section-band--linen .card h3,
	.section-band--slate .card h3,
	.section-band--butter .card h3 {
		color: #D4AF37;
	}
	.section-band--linen .card p,
	.section-band--slate .card p,
	.section-band--butter .card p {
		color: rgba(255, 255, 255, 0.85);
	}
	.card::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 2px;
		background: linear-gradient(90deg, #D4AF37, #E6C357, #D4AF37);
		opacity: 0;
		transition: opacity 0.3s ease;
	}
	.card:hover {
		transform: translateY(-8px);
		box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15), 0 0 0 1px rgba(212, 175, 55, 0.3);
		border-top-color: #D4AF37;
		background: linear-gradient(135deg, rgba(255, 255, 255, 1), rgba(252, 252, 252, 0.98));
	}
	.section-band--linen .card:hover,
	.section-band--slate .card:hover,
	.section-band--butter .card:hover {
		background: linear-gradient(135deg, rgba(26, 26, 26, 1), rgba(45, 45, 45, 0.98));
		box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4), 0 0 0 1px rgba(212, 175, 55, 0.3);
	}
	.card:hover::before {
		opacity: 1;
	}
	.card:nth-child(1) {
		animation-delay: 0.05s;
	}
	.card:nth-child(2) {
		animation-delay: 0.12s;
	}
	.card:nth-child(3) {
		animation-delay: 0.19s;
	}
	.card:nth-child(4) {
		animation-delay: 0.26s;
	}
	.card:nth-child(5) {
		animation-delay: 0.33s;
	}
	.card:nth-child(6) {
		animation-delay: 0.4s;
	}
	.card h3 {
		margin-bottom: 0.8em;
		word-break: normal;
		hyphens: auto;
		font-weight: 800;
		font-size: 1.2em;
		color: #000000;
		text-transform: uppercase;
		letter-spacing: 0.05em;
		line-height: 1.2;
	}
	.card p {
		margin: 0;
		line-height: 1.6;
		color: rgba(0, 0, 0, 0.75);
		font-weight: 500;
		font-size: 0.95em;
	}
	@keyframes riseFade {
		from {
			opacity: 0;
			transform: translateY(18px);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}
	.showcase-grid {
		display: grid;
		grid-template-columns: 1.6fr 1fr;
		gap: 1em;
	}
	.showcase-card {
		background: linear-gradient(135deg, rgba(26, 26, 26, 0.85), rgba(45, 45, 45, 0.9));
		border-radius: 0;
		border: 1px solid rgba(212, 175, 55, 0.2);
		box-shadow: 0 16px 32px rgba(0, 0, 0, 0.3), 0 0 0 1px rgba(212, 175, 55, 0.2);
		overflow: hidden;
		display: grid;
		grid-template-rows: auto 1fr;
		min-height: 100%;
		transition: all 0.3s ease;
		position: relative;
	}
	.showcase-card::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 2px;
		background: linear-gradient(90deg, #D4AF37, #E6C357, #D4AF37);
		opacity: 0;
		transition: opacity 0.3s ease;
	}
	.showcase-card:hover {
		transform: translateY(-8px);
		box-shadow: 0 24px 48px rgba(0, 0, 0, 0.15), 0 0 0 1px rgba(212, 175, 55, 0.3);
	}
	.showcase-card:hover::before {
		opacity: 1;
	}
	.showcase-card img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		display: block;
	}
	.showcase-card figcaption {
		margin: 0;
		padding: 1.2em 1.4em;
		font-weight: 700;
		color: #D4AF37;
		background: linear-gradient(180deg, rgba(0, 0, 0, 0.3), rgba(0, 0, 0, 0.1));
		font-size: 0.95em;
		line-height: 1.4;
	}
	.showcase-card--wide {
		grid-column: 1 / -1;
		display: grid;
		grid-template-columns: 2fr 1fr;
	}
	.showcase-card--wide img {
		aspect-ratio: 16 / 9;
		min-height: 100%;
	}
	.showcase-card--wide figcaption {
		display: flex;
		align-items: center;
		font-size: 1.1em;
		padding: 1.5em;
		font-weight: 800;
		letter-spacing: 0.05em;
	}
	.showcase-card--vertical img {
		aspect-ratio: 3 / 4;
	}
	.steps {
		position: relative;
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 1.4em;
		padding: 0.4em;
	}
	.steps::before {
		content: "";
		position: absolute;
		inset: 10% 8% auto 8%;
		height: 2px;
		background: linear-gradient(90deg, rgba(var(--gray), 0.16), rgba(var(--ring), 0.42), rgba(var(--gray), 0.16));
		z-index: 0;
	}
	.step {
		position: relative;
		display: grid;
		grid-template-columns: auto 1fr;
		gap: 1.2em;
		align-items: start;
		padding: 1.8em 1.5em;
		border-radius: 0;
		border: 1px solid rgba(212, 175, 55, 0.2);
		background: linear-gradient(180deg, rgba(255, 255, 255, 0.98), rgba(250, 250, 250, 0.95));
		box-shadow: 0 12px 24px rgba(0, 0, 0, 0.1), 0 0 0 1px rgba(212, 175, 55, 0.1);
		overflow: hidden;
		transition: all 0.3s ease;
	}
	.step::before {
		content: "";
		position: absolute;
		inset: 0;
		background: linear-gradient(120deg, rgba(212, 175, 55, 0.1), rgba(0, 0, 0, 0.05));
		opacity: 0;
		transition: opacity 0.3s ease;
		z-index: 0;
	}
	.step::after {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 2px;
		background: linear-gradient(90deg, #D4AF37, #E6C357, #D4AF37);
		opacity: 0;
		transition: opacity 0.3s ease;
	}
	.step:hover {
		transform: translateY(-6px);
		box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15), 0 0 0 1px rgba(212, 175, 55, 0.3);
		border-color: #D4AF37;
	}
	.step:hover::before {
		opacity: 1;
	}
	.step:hover::after {
		opacity: 1;
	}
	.step__number {
		position: relative;
		width: 48px;
		height: 48px;
		border-radius: 0;
		background: linear-gradient(135deg, #D4AF37 0%, #B8941F 100%);
		color: #000000;
		display: grid;
		place-items: center;
		font-weight: 900;
		font-size: 1.1em;
		box-shadow: 0 8px 16px rgba(212, 175, 55, 0.3), 0 0 0 1px rgba(212, 175, 55, 0.2);
		animation: float 4s ease-in-out infinite;
		z-index: 1;
		text-transform: uppercase;
		letter-spacing: 0.05em;
	}
	.step__number::after {
		content: "";
		position: absolute;
		inset: -4px;
		border-radius: 0;
		border: 2px solid rgba(212, 175, 55, 0.4);
		animation: pulse 2.8s ease-in-out infinite;
	}
	.step h3 {
		font-weight: 800;
		font-size: 1.1em;
		color: #000000;
		text-transform: uppercase;
		letter-spacing: 0.05em;
		margin-bottom: 0.5em;
		line-height: 1.2;
	}
	.step p {
		color: rgba(0, 0, 0, 0.75);
		font-weight: 500;
		line-height: 1.6;
		font-size: 0.95em;
	}
	.section-band--charcoal .step {
		background: linear-gradient(180deg, rgba(26, 26, 26, 0.95), rgba(45, 45, 45, 0.98));
		border: 1px solid rgba(212, 175, 55, 0.3);
		box-shadow: 0 12px 24px rgba(0, 0, 0, 0.4), 0 0 0 1px rgba(212, 175, 55, 0.2);
	}
	.section-band--charcoal .step h3 {
		color: #D4AF37;
	}
	.section-band--charcoal .step p {
		color: rgba(255, 255, 255, 0.85);
	}
	@keyframes float {
		0%,
		100% {
			transform: translateY(0px);
		}
		50% {
			transform: translateY(-4px);
		}
	}
	@keyframes pulse {
		0% {
			opacity: 0.6;
			transform: scale(1);
		}
		50% {
			opacity: 0.18;
			transform: scale(1.14);
		}
		100% {
			opacity: 0.6;
			transform: scale(1);
		}
	}
	.panel {
		background: linear-gradient(135deg, rgba(26, 26, 26, 0.85), rgba(45, 45, 45, 0.9));
		border: 1px solid rgba(212, 175, 55, 0.3);
		border-radius: 0;
		padding: 2.8em;
		box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3), 0 0 0 1px rgba(212, 175, 55, 0.2);
		position: relative;
	}
	.panel::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 2px;
		background: linear-gradient(90deg, #D4AF37, #E6C357, #D4AF37);
	}
	.two-col {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
		gap: 1em;
	}
	.cta {
		display: grid;
		grid-template-columns: 2fr 1fr;
		align-items: center;
		gap: 2em;
		background: linear-gradient(135deg, #000000 0%, #1a1a1a 100%);
		color: #FFFFFF;
		border-radius: 0;
		padding: 2.5em;
		box-shadow: 0 24px 48px rgba(0, 0, 0, 0.4), 0 0 0 1px rgba(212, 175, 55, 0.2);
		border: 2px solid rgba(212, 175, 55, 0.3);
		position: relative;
		overflow: hidden;
	}
	.cta::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 3px;
		background: linear-gradient(90deg, #D4AF37, #E6C357, #D4AF37);
	}
	.cta h2 {
		color: #FFFFFF;
		font-weight: 900;
		text-transform: uppercase;
		letter-spacing: 0.05em;
		font-size: 1.8em;
		line-height: 1.1;
	}
	.cta__actions {
		display: grid;
		gap: 1em;
	}
	.cta__actions .btn {
		width: 100%;
		text-align: center;
		padding: 1em 1.5em;
		font-weight: 800;
		text-transform: uppercase;
		letter-spacing: 0.08em;
		border-radius: 0;
		transition: all 0.3s ease;
	}
	.cta__actions .btn.primary {
		background: rgba(212, 175, 55, 0.15);
		border: 2px solid #D4AF37;
		color: #FFFFFF;
	}
	.cta__actions .btn.primary:hover {
		background: rgba(212, 175, 55, 0.25);
		border-color: #E6C357;
		color: #FFFFFF;
	}
	.cta__actions .btn.ghost {
		background: transparent;
		border: 2px solid rgba(212, 175, 55, 0.6);
		color: #D4AF37;
	}
	.cta__actions .btn.ghost:hover {
		background: rgba(212, 175, 55, 0.1);
		border-color: #D4AF37;
		color: #FFFFFF;
	}
	.cta__address {
		margin: 0;
		color: rgba(212, 175, 55, 0.9);
		font-weight: 600;
		text-transform: uppercase;
		letter-spacing: 0.05em;
		font-size: 0.9em;
	}
	.copy-row {
		display: flex;
		gap: 0.4em;
		flex-wrap: wrap;
	}
	.copy-btn {
		border: 1px solid rgba(212, 175, 55, 0.3);
		background: rgba(0, 0, 0, 0.8);
		color: #D4AF37;
		border-radius: 0;
		padding: 0.6em 1em;
		font-weight: 700;
		cursor: pointer;
		font-size: 0.85em;
		text-transform: uppercase;
		letter-spacing: 0.05em;
		transition: all 0.3s ease;
	}
	.copy-btn:hover {
		border-color: #D4AF37;
		color: #FFFFFF;
		background: rgba(212, 175, 55, 0.2);
		box-shadow: 0 4px 12px rgba(212, 175, 55, 0.2);
	}
	main section {
		margin-top: 4em;
	}
	.hero--banner + section {
		margin-top: 1.5em;
	}
	.section-flair {
		position: relative;
		overflow: hidden;
	}
	.section-flair::before {
		content: "";
		position: absolute;
		inset: -10% -8%;
		background-repeat: no-repeat;
		background-position: center;
		background-size: 120% 120%;
		opacity: 0.65;
		pointer-events: none;
		z-index: 1;
	}
	.section-flair > * {
		position: relative;
		z-index: 2;
	}
	.section-flair--warm::before {
		background-image:
			linear-gradient(145deg, rgba(var(--ring), 0.14), rgba(var(--ring), 0)),
			linear-gradient(12deg, rgba(255, 255, 255, 0.07), rgba(0, 0, 0, 0.04));
	}
	.section-flair--grid::before {
		background-image:
			repeating-linear-gradient(90deg, rgba(var(--ring), 0.14) 0, rgba(var(--ring), 0.14) 10px, transparent 10px, transparent 28px),
			linear-gradient(120deg, rgba(255, 255, 255, 0.06), rgba(0, 0, 0, 0.03));
		opacity: 0.5;
	}
	.section-flair--quiet::before {
		background-image:
			linear-gradient(180deg, rgba(122, 166, 184, 0.08), rgba(122, 166, 184, 0)),
			linear-gradient(25deg, rgba(0, 0, 0, 0.03), rgba(255, 255, 255, 0.06));
		opacity: 0.45;
	}
	@media (max-width: 960px) {
		.hero,
		.cta {
			grid-template-columns: 1fr;
		}
		:root {
			--hero-lift: -120px;
		}
		.hero-strip {
			margin: 0 calc(50% - 50vw);
			border-radius: 14px;
			height: 280px;
		}
		.hero--banner {
			padding: 1.6em 1.2em;
		}
		.hero__content .lede {
			max-width: 100%;
		}
		.steps {
			grid-template-columns: 1fr;
		}
		.steps::before {
			display: none;
		}
		.copy-row {
			justify-content: flex-start;
		}
		.card-grid {
			grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
		}
		.showcase-grid {
			grid-template-columns: 1fr;
		}
		.showcase-card--wide {
			grid-template-columns: 1fr;
		}
		.showcase-card--wide figcaption {
			padding: 1em;
		}
	}
</style>
<script type="module">
	if (typeof window !== "undefined") {
		const copyButtons = Array.from(document.querySelectorAll("[data-copy]"));
		const fallbackCopy = (text) => {
			const area = document.createElement("textarea");
			area.value = text;
			document.body.appendChild(area);
			area.select();
			document.execCommand("copy");
			document.body.removeChild(area);
		};
		const copyText = async (text) => {
			if (navigator.clipboard?.writeText) {
				try {
					await navigator.clipboard.writeText(text);
					return true;
				} catch (err) {
					fallbackCopy(text);
					return false;
				}
			}
			fallbackCopy(text);
			return false;
		};
		copyButtons.forEach((btn) => {
			const defaultLabel = btn.textContent;
			btn.addEventListener("click", async () => {
				const text = btn.getAttribute("data-copy") || "";
				await copyText(text);
				btn.textContent = "Copied!";
				setTimeout(() => {
					btn.textContent = defaultLabel;
				}, 1200);
			});
		});
	}
</script>
