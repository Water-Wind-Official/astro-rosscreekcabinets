---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main id="home">
			<section class="hero-strip" aria-hidden="true">
				<img src="https://i.postimg.cc/CLLdWd5W/Gemini-Generated-Image-s6035cs6035cs603.png" alt="" loading="lazy" decoding="async" />
			</section>
			<section class="hero hero--banner section-flair section-flair--warm section-band section-band--amber">
				<div class="hero__content">
					<p class="eyebrow">Waycross, GA • Family owned</p>
					<h1>Cabinets built for the way you live.</h1>
					<p class="lede">
						Ross Creek Cabinets designs and installs kitchens, vanities, built-ins, and storage for South Georgia
						homes.
					</p>
					<p class="lede alt">
						Owner Derek Roberson measures, builds, and installs so every cabinet fits, functions, and looks right on
						day one.
					</p>
					<div class="hero__actions">
						<a class="btn primary" href="#contact">912-337-1152</a>
						<a class="btn ghost" href="#contact">Email</a>
					</div>
					<ul class="hero__highlights">
						<li>Made-to-measure layouts</li>
						<li>Built and installed locally by Derek</li>
						<li>Durable woods, finishes, and hardware</li>
					</ul>
				</div>
				<div class="hero__card">
					<h3>Meet your cabinetmaker</h3>
					<p>
						“Every project is personal. I measure, build, and install so you get cabinetry that fits right
						and looks right.”
					</p>
					<div class="hero__contact">
						<p>Derek Roberson</p>
						<p>Ross Creek Cabinets LLC</p>
						<p>Waycross, Georgia</p>
					</div>
				</div>
			</section>

			<section id="services" class="section-flair section-flair--grid section-band section-band--linen">
				<div class="section-header">
					<p class="eyebrow">What we build</p>
					<h2>Cabinetry tailored to your rooms and routines.</h2>
					<p>
						From first sketch to final install, we design around your dimensions, finishes, and storage needs.
						We can coordinate with your contractor or manage the full cabinetry scope.
					</p>
				</div>
				<div class="card-grid">
					<div class="card">
						<h3>Kitchens</h3>
						<p>Full custom kitchens, islands, pantries, and trim that anchor your home.</p>
					</div>
					<div class="card">
						<h3>Bathrooms &amp; Laundry</h3>
						<p>Vanities, linen towers, and laundry storage built to handle daily use.</p>
					</div>
					<div class="card">
						<h3>Built-ins &amp; Living Spaces</h3>
						<p>Fireplace surrounds, media walls, mudrooms, and window seats that look original.</p>
					</div>
					<div class="card">
						<h3>Closets &amp; Storage</h3>
						<p>Closet systems, drop zones, and organization that keep everything in its place.</p>
					</div>
					<div class="card">
						<h3>Commercial Spaces</h3>
						<p>Reception desks, break rooms, and storage that work for busy offices.</p>
					</div>
					<div class="card">
						<h3>Finishes &amp; Hardware</h3>
						<p>Paint-grade or stained hardwoods, soft-close hardware, pull-outs, and organizers.</p>
					</div>
				</div>
			</section>

			<section id="process" class="section-creek section-band section-band--charcoal">
				<div class="section-header">
					<p class="eyebrow">How it works</p>
					<h2>A clear path from first call to final install.</h2>
				</div>
				<div class="steps">
					<div class="step">
						<div class="step__number">1</div>
						<div>
							<h3>Measure &amp; consult</h3>
							<p>We meet on-site in Waycross, GA, confirm dimensions, talk finishes, and align on budget.</p>
						</div>
					</div>
					<div class="step">
						<div class="step__number">2</div>
						<div>
							<h3>Design &amp; approval</h3>
							<p>Layouts and door styles are finalized; you review details before we start building.</p>
						</div>
					</div>
					<div class="step">
						<div class="step__number">3</div>
						<div>
							<h3>Build</h3>
							<p>We source materials, craft in the shop, pre-fit, and finish for a clean install.</p>
						</div>
					</div>
					<div class="step">
						<div class="step__number">4</div>
						<div>
							<h3>Install &amp; walkthrough</h3>
							<p>Cabinets are installed, aligned, and tuned. We walk through together and handle any touch-ups.</p>
						</div>
					</div>
				</div>
			</section>

			<section id="portfolio" class="section-flair section-flair--quiet showcase section-band section-band--slate">
				<div class="section-header">
					<p class="eyebrow">Recent work</p>
					<h2>Cabinetry sized for the room, styled for daily living.</h2>
					<p>
						A few in-progress shots from the shop and job sites. Each piece is measured, built, and finished by
						Derek so the fit and finish stay consistent from drawings to install.
					</p>
				</div>
				<div class="showcase-grid">
					<figure class="showcase-card showcase-card--wide">
						<img src="https://i.postimg.cc/59LjDS64/IMG-20251209-141756.jpg" alt="Light, warm kitchen cabinetry with a centered island" loading="lazy" />
						<figcaption>Kitchen run with a painted island, wide drawers, and balanced uppers.</figcaption>
					</figure>
					<figure class="showcase-card showcase-card--vertical">
						<img src="https://i.postimg.cc/QMXh2TYC/IMG-20251209-141801.jpg" alt="Tall built-in cabinetry with open shelves" loading="lazy" />
						<figcaption>Built-in shelving and base storage sized to the wall for a clean fit.</figcaption>
					</figure>
					<figure class="showcase-card showcase-card--vertical">
						<img src="https://i.postimg.cc/YjKwbGm0/IMG-20251209-141751.jpg" alt="Detailed cabinet doors staged for finishing" loading="lazy" />
						<figcaption>Door sets prefinished and staged before hardware and installation.</figcaption>
					</figure>
				</div>
			</section>

			<section id="about" class="panel section-flair section-flair--warm section-band section-band--butter">
				<div class="section-header">
					<p class="eyebrow">Who we are</p>
					<h2>Owner-led craftsmanship in Waycross, Georgia.</h2>
					<p>
						Ross Creek Cabinets LLC is owned and operated by Derek Roberson. Every project is measured,
						built, and installed with care so your cabinetry fits the room, functions for daily life, and
						looks good for years.
					</p>
				</div>
				<div class="two-col">
					<div class="card">
						<h3>Local and accountable</h3>
						<p>Located at 2116 Carswell Ave, Waycross, GA. We serve homeowners, remodelers, and builders across South Georgia.</p>
					</div>
					<div class="card">
						<h3>Communication first</h3>
						<p>One point of contact from estimate to install. You always know what’s next and when to expect it.</p>
					</div>
				</div>
			</section>

			<section id="contact" class="section-flair section-flair--quiet section-band section-band--creek">
				<div class="cta">
					<div>
						<p class="eyebrow">Ready to start?</p>
						<h2>Tell us about your project.</h2>
						<p>Call or email to schedule a site visit in Waycross or the surrounding South Georgia area.</p>
					</div>
					<div class="cta__actions">
						<a class="btn primary" href="#contact">912-337-1152</a>
						<a class="btn ghost" href="mailto:rosscreekcabinets@gmail.com">rosscreekcabinets@gmail.com</a>
						<p class="cta__address">2116 Carswell Ave • Waycross, GA</p>
						<div class="copy-row">
							<button class="copy-btn" type="button" data-copy="+19123371152" aria-label="Copy phone number">Copy phone</button>
							<button class="copy-btn" type="button" data-copy="rosscreekcabinets@gmail.com" aria-label="Copy email address">Copy email</button>
						</div>
					</div>
				</div>
			</section>
		</main>
		<Footer />
	</body>
</html>
<style>
	.hero {
		display: grid;
		grid-template-columns: 2fr 1fr;
		gap: 2em;
		align-items: center;
	}
	:root {
		--hero-lift: -440px;
	}
	.hero-strip {
		position: relative;
		margin: 0 calc(50% - 50vw);
		overflow: hidden;
		border-radius: 18px;
		box-shadow: 0 18px 34px rgba(var(--black), 0.18);
	}
	.hero-strip img {
		width: 100vw;
		max-width: 100%;
		height: 620px;
		display: block;
		object-fit: cover;
		object-position: center;
	}
	.hero-strip::after {
		content: "";
		position: absolute;
		inset: 0;
		background: linear-gradient(180deg, rgba(24, 32, 44, 0.08), rgba(24, 32, 44, 0.45));
		pointer-events: none;
	}
	main > section:first-of-type {
		margin-top: 0;
	}
	.hero--banner {
		position: relative;
		margin-top: var(--hero-lift);
		padding: 2.4em 2.2em;
		overflow: hidden;
		border-radius: 18px;
		box-shadow: 0 18px 34px rgba(var(--black), 0.18);
		background: linear-gradient(135deg, rgba(255, 255, 255, 0.94), rgba(247, 243, 236, 0.96));
		z-index: 2;
	}
	.hero__content {
		background: #fdfbf7;
		padding: 1.6em;
		border-radius: 18px;
		border: 1px solid rgba(var(--gray), 0.12);
		box-shadow: 0 14px 32px rgba(var(--gray), 0.1);
	}
	.hero__content h1 {
		margin-bottom: 0.35em;
		max-width: 22ch;
		line-height: 1.15;
	}
	.hero__content .lede {
		max-width: 50ch;
		margin-bottom: 0.6em;
	}
	.hero__content .lede.alt {
		color: rgb(var(--gray));
		margin-top: -0.2em;
	}
	.hero__actions {
		display: flex;
		flex-wrap: wrap;
		gap: 0.6em;
		margin: 1em 0;
	}
	.hero__highlights {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
		gap: 0.5em;
		list-style: none;
		padding: 0;
		margin: 1em 0 0 0;
	}
	.hero__highlights li {
		background: rgba(var(--gray-light), 0.5);
		padding: 0.6em 0.8em;
		border-radius: 10px;
		border: 1px solid rgba(var(--gray), 0.12);
		font-weight: 600;
	}
	.hero__card {
		background: var(--surface);
		padding: 1.5em;
		border-radius: 16px;
		box-shadow: var(--box-shadow);
		border: 1px solid rgba(var(--gray), 0.12);
	}
	.hero__card h3 {
		margin-bottom: 0.4em;
	}
	.hero__contact {
		margin-top: 1em;
		padding-top: 1em;
		border-top: 1px solid rgba(var(--gray-light), 0.8);
		color: rgb(var(--gray));
	}
	.eyebrow {
		text-transform: uppercase;
		letter-spacing: 0.08em;
		font-weight: 700;
		font-size: 0.85em;
		color: var(--accent);
		margin: 0 0 0.25em 0;
	}
	.section-header {
		text-align: left;
		max-width: 820px;
		margin-bottom: 2em;
	}
	.section-band {
		position: relative;
	}
	.section-band::before {
		content: "";
		position: absolute;
		inset: -10% -9999px;
		background: var(--section-band-color, #f9f3e7);
		z-index: 0;
	}
	.section-band > * {
		position: relative;
		z-index: 2;
	}
	.section-band--amber {
		--section-band-color: linear-gradient(130deg, rgba(154, 97, 42, 0.2), rgba(255, 255, 255, 0.9));
	}
	.section-band--linen {
		--section-band-color: linear-gradient(135deg, rgba(237, 240, 246, 0.7), rgba(245, 241, 234, 0.95));
	}
	.section-band--creek {
		--section-band-color: linear-gradient(150deg, rgba(122, 166, 184, 0.2), rgba(245, 241, 234, 0.92));
	}
	.section-band--slate {
		--section-band-color: linear-gradient(150deg, rgba(96, 115, 159, 0.18), rgba(240, 236, 230, 0.9));
	}
	.section-band--butter {
		--section-band-color: linear-gradient(140deg, rgba(255, 235, 205, 0.5), rgba(247, 243, 236, 0.96));
	}
	.section-band--charcoal {
		--section-band-color: linear-gradient(180deg, rgba(24, 32, 44, 0.94), rgba(36, 45, 60, 0.9));
	}
	.section-band--shell {
		--section-band-color: linear-gradient(135deg, rgba(255, 255, 255, 0.92), rgba(122, 166, 184, 0.12));
	}
	.card-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
		gap: 2em;
	}
	.card {
		background: var(--surface);
		padding: 1.6em;
		border-radius: 14px;
		border: 2px solid rgba(var(--gray), 0.1);
		box-shadow: 0 12px 26px rgba(var(--gray), 0.08);
	}
	.showcase-grid {
		display: grid;
		grid-template-columns: 1.6fr 1fr;
		gap: 1em;
	}
	.showcase-card {
		background: var(--surface);
		border-radius: 16px;
		border: 1px solid rgba(var(--gray), 0.12);
		box-shadow: 0 16px 28px rgba(var(--gray), 0.12);
		overflow: hidden;
		display: grid;
		grid-template-rows: auto 1fr;
		min-height: 100%;
	}
	.showcase-card img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		display: block;
	}
	.showcase-card figcaption {
		margin: 0;
		padding: 0.9em 1em;
		font-weight: 650;
		color: rgb(var(--gray-dark));
		background: linear-gradient(180deg, rgba(154, 97, 42, 0.06), rgba(154, 97, 42, 0));
	}
	.showcase-card--wide {
		grid-column: 1 / -1;
		display: grid;
		grid-template-columns: 2fr 1fr;
	}
	.showcase-card--wide img {
		aspect-ratio: 16 / 9;
		min-height: 100%;
	}
	.showcase-card--wide figcaption {
		display: flex;
		align-items: center;
		font-size: 1.05em;
		padding: 1.2em;
	}
	.showcase-card--vertical img {
		aspect-ratio: 3 / 4;
	}
	.steps {
		position: relative;
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 1.4em;
		padding: 0.4em;
	}
	.steps::before {
		content: "";
		position: absolute;
		inset: 10% 8% auto 8%;
		height: 2px;
		background: linear-gradient(90deg, rgba(var(--gray), 0.16), rgba(154, 97, 42, 0.28), rgba(var(--gray), 0.16));
		z-index: 0;
	}
	.step {
		position: relative;
		display: grid;
		grid-template-columns: auto 1fr;
		gap: 0.9em;
		align-items: start;
		padding: 1.25em 1.35em;
		border-radius: 14px;
		border: 1px solid rgba(var(--gray), 0.14);
		background: linear-gradient(180deg, rgba(255, 255, 255, 0.92), rgba(245, 241, 234, 0.96));
		box-shadow: 0 12px 24px rgba(var(--gray), 0.12);
		overflow: hidden;
		transition: transform 0.2s ease, box-shadow 0.2s ease;
	}
	.step::before {
		content: "";
		position: absolute;
		inset: 0;
		background: linear-gradient(120deg, rgba(154, 97, 42, 0.12), rgba(122, 166, 184, 0.08));
		opacity: 0;
		transition: opacity 0.25s ease;
		z-index: 0;
	}
	.step:hover {
		transform: translateY(-4px);
		box-shadow: 0 16px 32px rgba(var(--gray), 0.16);
	}
	.step:hover::before {
		opacity: 1;
	}
	.step__number {
		position: relative;
		width: 40px;
		height: 40px;
		border-radius: 50%;
		background: radial-gradient(circle at 30% 30%, rgba(255, 255, 255, 0.4), rgba(154, 97, 42, 0.4)), linear-gradient(145deg, var(--accent), var(--accent-dark));
		color: #fff;
		display: grid;
		place-items: center;
		font-weight: 800;
		box-shadow: 0 10px 18px rgba(var(--accent), 0.28);
		animation: float 4s ease-in-out infinite;
		z-index: 1;
	}
	.step__number::after {
		content: "";
		position: absolute;
		inset: -6px;
		border-radius: 50%;
		border: 2px solid rgba(var(--accent), 0.35);
		animation: pulse 2.8s ease-in-out infinite;
	}
	@keyframes float {
		0%,
		100% {
			transform: translateY(0px);
		}
		50% {
			transform: translateY(-4px);
		}
	}
	@keyframes pulse {
		0% {
			opacity: 0.6;
			transform: scale(1);
		}
		50% {
			opacity: 0.18;
			transform: scale(1.14);
		}
		100% {
			opacity: 0.6;
			transform: scale(1);
		}
	}
	.panel {
		background: #fbf6ef;
		border: 1px solid rgba(var(--accent), 0.12);
		border-radius: 18px;
		padding: 2.3em;
		box-shadow: 0 16px 30px rgba(var(--gray), 0.08);
	}
	.two-col {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
		gap: 1em;
	}
	.cta {
		display: grid;
		grid-template-columns: 2fr 1fr;
		align-items: center;
		gap: 1.2em;
		background: linear-gradient(135deg, var(--accent), var(--accent-dark));
		color: #fff;
		border-radius: 18px;
		padding: 1.8em;
		box-shadow: var(--box-shadow);
	}
	.cta h2 {
		color: #fff;
	}
	.cta__actions {
		display: grid;
		gap: 0.6em;
	}
	.cta__actions .btn {
		width: 100%;
		text-align: center;
	}
	.cta__address {
		margin: 0;
		color: rgba(255, 255, 255, 0.85);
	}
	.btn {
		display: inline-block;
		padding: 0.75em 1.1em;
		border-radius: 999px;
		font-weight: 700;
		text-decoration: none;
		border: 2px solid var(--accent);
	}
	.btn.primary {
		background: var(--accent);
		color: #fff;
	}
	.btn.primary:hover {
		background: var(--accent-dark);
		border-color: var(--accent-dark);
		color: #fff;
	}
	.btn.ghost {
		background: var(--surface);
		color: var(--accent);
	}
	.btn.ghost:hover {
		background: #fff5eb;
	}
	.copy-row {
		display: flex;
		gap: 0.4em;
		flex-wrap: wrap;
	}
	.copy-btn {
		border: 1px solid rgba(var(--gray), 0.25);
		background: #fff;
		color: rgb(var(--gray-dark));
		border-radius: 10px;
		padding: 0.4em 0.7em;
		font-weight: 600;
		cursor: pointer;
		font-size: 0.9em;
		transition: border-color 0.2s ease, color 0.2s ease, box-shadow 0.2s ease;
	}
	.copy-btn:hover {
		border-color: var(--accent);
		color: var(--accent);
		box-shadow: 0 4px 12px rgba(var(--gray), 0.12);
	}
	main section {
		margin-top: 3em;
	}
	.section-flair {
		position: relative;
		overflow: hidden;
	}
	.section-flair::before {
		content: "";
		position: absolute;
		inset: -10% -8%;
		background-repeat: no-repeat;
		background-position: center;
		background-size: 120% 120%;
		opacity: 0.65;
		pointer-events: none;
		z-index: 1;
	}
	.section-flair > * {
		position: relative;
		z-index: 2;
	}
	.section-flair--warm::before {
		background-image:
			linear-gradient(145deg, rgba(154, 97, 42, 0.09), rgba(154, 97, 42, 0)),
			linear-gradient(12deg, rgba(255, 255, 255, 0.07), rgba(0, 0, 0, 0.04));
	}
	.section-flair--grid::before {
		background-image:
			repeating-linear-gradient(90deg, rgba(154, 97, 42, 0.08) 0, rgba(154, 97, 42, 0.08) 10px, transparent 10px, transparent 28px),
			linear-gradient(120deg, rgba(255, 255, 255, 0.06), rgba(0, 0, 0, 0.03));
		opacity: 0.5;
	}
	.section-flair--quiet::before {
		background-image:
			linear-gradient(180deg, rgba(122, 166, 184, 0.08), rgba(122, 166, 184, 0)),
			linear-gradient(25deg, rgba(0, 0, 0, 0.03), rgba(255, 255, 255, 0.06));
		opacity: 0.45;
	}
	@media (max-width: 960px) {
		.hero,
		.cta {
			grid-template-columns: 1fr;
		}
		:root {
			--hero-lift: -70px;
		}
		.hero-strip {
			margin: 0 calc(50% - 50vw);
			border-radius: 14px;
			height: 280px;
		}
		.hero--banner {
			margin-top: -48px;
			padding: 1.6em 1.2em;
		}
		.hero__content .lede {
			max-width: 100%;
		}
		.steps {
			grid-template-columns: 1fr;
		}
		.steps::before {
			display: none;
		}
		.copy-row {
			justify-content: flex-start;
		}
		.showcase-grid {
			grid-template-columns: 1fr;
		}
		.showcase-card--wide {
			grid-template-columns: 1fr;
		}
		.showcase-card--wide figcaption {
			padding: 1em;
		}
	}
</style>
<script type="module">
	if (typeof window !== "undefined") {
		const copyButtons = Array.from(document.querySelectorAll("[data-copy]"));
		const fallbackCopy = (text) => {
			const area = document.createElement("textarea");
			area.value = text;
			document.body.appendChild(area);
			area.select();
			document.execCommand("copy");
			document.body.removeChild(area);
		};
		const copyText = async (text) => {
			if (navigator.clipboard?.writeText) {
				try {
					await navigator.clipboard.writeText(text);
					return true;
				} catch (err) {
					fallbackCopy(text);
					return false;
				}
			}
			fallbackCopy(text);
			return false;
		};
		copyButtons.forEach((btn) => {
			const defaultLabel = btn.textContent;
			btn.addEventListener("click", async () => {
				const text = btn.getAttribute("data-copy") || "";
				await copyText(text);
				btn.textContent = "Copied!";
				setTimeout(() => {
					btn.textContent = defaultLabel;
				}, 1200);
			});
		});
	}
</script>
